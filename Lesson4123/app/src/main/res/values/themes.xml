<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Lesson412" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/purple_700</item>
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>

    <style name="MyActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/purple_500</item>
    </style>

    <style name="Theme.Lesson412" parent="Base.Theme.Lesson412" />
</resources>